import "./SearchButton.css";

export default function SearchButton() {
  return (
    <button
      className="search-button"
      aria-label="Cari"
      type="button"
    >
      🔍
    </button>
  );
}